import React from "react";
import "../scss/main.scss";

const App = (): JSX.Element => {
  return (
    <React.StrictMode>
      <div className="container">App</div>
    </React.StrictMode>
  );
};

export default App;
